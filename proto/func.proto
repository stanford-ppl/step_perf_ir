syntax = "proto3";

package step_perf_ir;

message ElemtoElemFunc {
  oneof elem_elem_fn { Matmul matmul = 1; }
}

message Matmul { bool weight_transposed = 1; }

message InitFunc {
  oneof init_fn { Zero zero = 1; }
}

message Zero {}